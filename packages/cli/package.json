{
  "name": "@iceberg/cli",
  "version": "0.1.0",
  "private": false,
  "source": "src/index.js",
  "main": "dist/index.js",
  "bin": {
    "iceberg": "bin/iceberg.js"
  },
  "files": [
    "dist",
    "bin"
  ],
  "scripts": {
    "build": "microbundle --format cjs --no-generateTypes --no-compress --external none",
    "dev": "microbundle watch --format cjs --no-generateTypes --no-compress --external none",
    "lint": "biome check ./src ./bin",
    "test": "node bin/iceberg.js --help"
  },
  "dependencies": {
    "@darkobits/lolcatjs": "^3.1.3",
    "commander": "^11.1.0",
    "figlet": "^1.6.0",
    "inquirer": "^9.2.12",
    "ora": "^7.0.1",
    "quicktype-core": "^23.0.170",
    "shelljs": "^0.8.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "microbundle": "^0.15.1"
  }
}
